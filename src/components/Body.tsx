import * as React from 'react';
import styled from 'styled-components';

export default () => (
  <Body>
    Hiya. I'm a software engineer based in London. This site will eventually have more stuff, but for now, this is all.
  </Body>
);

const Body = styled.div`
  padding-top: 1rem;
  color: #999;
`;
